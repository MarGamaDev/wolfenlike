extends Node3D

@export var enemy_manager_script : EnemyManager

var csg_boxes : Array[Node]
var grid_size : int



# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
